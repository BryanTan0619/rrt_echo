# Base 0.1.0 状态

本版整理自 V30 开发实现。已实现：局部联合观察、有限预算对应、typed ownership、可重放观察日志、版本化超图、scoped retrieval 与 graph-only QA。

历史开发样例 `gryenlQKTbE`：45/45 窗口，349 条事实，188 个局部实例；正式建库 73 次模型调用。固定观察的绑定一致性修复前 QA 19/28，之后 20/28；每轮 QA 56 次调用。图内审计 27 题不足、1 题矛盾，未完成独立媒体支持验收。原始视频、标注与实验产物不随仓库分发。

| R 类型 | 最终正确/总题数 |
|---|---:|
| R1 | 4/6 |
| R2 | 3/4 |
| R3 | 3/4 |
| R4 | 1/4 |
| R5 | 5/5 |
| R6 | 2/3 |
| R7 | 2/2 |

角色约束使检查到的 13 个 carry/hold 双人物角色折叠降至 0，但隔离冲突簇也导致部分正确身份链断开。主要缺口仍为 OCR、区域/人物类型、跨镜头身份、动作发起角色、文字承载物/来源和近似时间边界。这是开发结果，不是泛化验收。

发布整理没有重新运行昂贵的视频模型实验，不改变保留模块的算法语义。旧离线架构、专用实验脚本、历史文档及相应测试保存在开发工作区的 `local_archive/`，不进入 Git；该归档不是安装/运行依赖。

发布检查：整理后保留的 158 项测试通过；旧架构专属测试随旧模块归档，未以减少测试数充当修复。Ruff 检查与文档本地链接检查通过。

安装检查：wheel/sdist 构建成功；独立环境中的 5 个命令入口、合成回放与 scoped payload 检查通过。使用安装后的 wheel 重建历史 V30 日志，快照 ID 与整理前一致。
